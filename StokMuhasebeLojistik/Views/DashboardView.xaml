<UserControl x:Class="StokMuhasebeLojistik.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:StokMuhasebeLojistik.Views">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Ana Sayfa" Style="{StaticResource HeaderTextStyle}"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Toplam Stok Kalemi" Style="{StaticResource SubHeaderTextStyle}"/>
                        <TextBlock Text="{Binding DashboardData[TotalStockItems]}" 
                                  FontSize="36" FontWeight="Bold" Foreground="#2196F3"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Toplam Araç" Style="{StaticResource SubHeaderTextStyle}"/>
                        <TextBlock Text="{Binding DashboardData[TotalVehicles]}" 
                                  FontSize="36" FontWeight="Bold" Foreground="#4CAF50"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Aktif Seferler" Style="{StaticResource SubHeaderTextStyle}"/>
                        <TextBlock Text="{Binding DashboardData[ActiveTrips]}" 
                                  FontSize="36" FontWeight="Bold" Foreground="#FF9800"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="3" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Toplam Müşteri" Style="{StaticResource SubHeaderTextStyle}"/>
                        <TextBlock Text="{Binding DashboardData[TotalCustomers]}" 
                                  FontSize="36" FontWeight="Bold" Foreground="#9C27B0"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <Border Style="{StaticResource CardStyle}" Margin="10,20,10,10">
                <StackPanel>
                    <TextBlock Text="Finansal Özet" Style="{StaticResource SubHeaderTextStyle}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="TL Bakiye" Style="{StaticResource LabelTextStyle}"/>
                            <TextBlock Text="{Binding DashboardData[FinancialSummary][TL], StringFormat='{}{0:C}'}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#2196F3"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="USD Bakiye" Style="{StaticResource LabelTextStyle}"/>
                            <TextBlock Text="{Binding DashboardData[FinancialSummary][USD], StringFormat='${0:F2}'}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="EUR Bakiye" Style="{StaticResource LabelTextStyle}"/>
                            <TextBlock Text="{Binding DashboardData[FinancialSummary][EUR], StringFormat='€{0:F2}'}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#FF9800"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
